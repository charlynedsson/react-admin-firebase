{
  "name": "react-admin-firebase-workspace",
  "private": true,
  "version": "1.0.0",
  "homepage": "https://github.com/charlynedsson/react-admin-firebase",
  "license": "MIT",
  "files": [
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "start-emulators": "firebase emulators:start --project react-admin-firebase",
    "start-demo": "npm run dev -w packages/demo",
    "test": "npm test -w packages/core",
    "watch": "npm run dev -w packages/demo && npm run watch -w packages/core"
  },
  "devDependencies": {
    "firebase-tools": "^13.x",
    "typescript": "^5.7.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
